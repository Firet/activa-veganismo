/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/vegan-volunteerism",
    output: "export",
    reactStrictMode: true,
    distDir: "docs",
};

export default nextConfig;
