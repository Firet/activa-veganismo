/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/docs',
    assetPrefix: '',
    output: "export",
    reactStrictMode: true,
    distDir: "docs",
};

export default nextConfig;
