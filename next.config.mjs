/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/vegan-volunteerism',
    assetPrefix: '',
    output: "export",
    reactStrictMode: true,
    // distDir: "docs",
};

export default nextConfig;
