/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '',
    assetPrefix: '',
    output: "export",
    reactStrictMode: true,
    // distDir: "docs",
};

export default nextConfig;
